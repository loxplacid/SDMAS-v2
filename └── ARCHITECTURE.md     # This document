
## Dependencies Flow
Presentation Layer → Application Layer → Domain Layer → Infrastructure Layer

Each layer depends only on layers closer to the core. No reverse dependencies are allowed.

## Security Considerations
- Input validation at all boundaries
- Principle of least privilege for services
- Secure data handling in repositories
- Authentication and authorization implemented at appropriate layers

This architecture ensures maintainability, testability, and scalability while enforcing clear separation of concerns.
