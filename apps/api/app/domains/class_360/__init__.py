"""Class 360 — cross-domain operational view for Classes/Sections.

Read-only aggregation endpoints that compose existing academic, attendance,
fees, workflow, and audit data into a single class-level 360 view.  No new
CRUD is introduced here — drill-down happens through the existing domain
APIs (sections, students, attendance, etc.).
"""
