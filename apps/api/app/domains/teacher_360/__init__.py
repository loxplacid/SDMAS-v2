"""Teacher 360 — cross-domain operational view for Teachers.

Read-only aggregation endpoints that compose existing academic, attendance,
leave, workflow, timetable, and audit data into a single teacher-level 360
view.  No new CRUD is introduced here — drill-down happens through the
existing domain APIs.
"""
