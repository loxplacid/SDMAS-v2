# SDMAS v2 — Release Manifest

**Purpose:** Defines the reproducible release candidate: what is source of
truth, what is generated (and excluded), what must ship, and what evidence the
release must carry.
**Prepared:** 2026-08-16
**Source tree:** `main` @ `77c72f8` + two pending documentation files
(`docs/enterprise/RELEASE-BASELINE.md`, this file) — no other working-tree
changes.

---

## 1. Release version / tag candidate

- **Version:** `v2.0.0-rc.1` (first tagged release — repository currently has
  **no tags**; `git tag -l` is empty).
- **Tag command (once cleanup in §6 is applied):**
  ```bash
  git tag -a v2.0.0-rc.1 -m "SDMAS v2 release candidate 1"
  ```
- **Source commit candidate:** `77c72f8` ("final updates") — the tree is
  clean except the two manifest/baseline docs, which become part of the tag.

## 2. Source-of-truth files (must be tracked — all verified present)

| Area | Verified tracked |
|---|---|
| Backend application | `apps/api/app/` (36 domain modules, core, infra, multi_tenant) |
| Backend manifests | `apps/api/pyproject.toml`, `apps/api/uv.lock`, `apps/api/alembic.ini`, `apps/api/alembic/env.py`, `apps/api/alembic/script.py.mako`, `apps/api/alembic/README` |
| **Migrations** | **all 56** files in `apps/api/alembic/versions/` — single head `049_widen_audit_action` |
| Seeders | `apps/api/scripts/seed.py`, `apps/api/scripts/seed_enterprise_demo.py`, `apps/api/scripts/__init__.py` |
| Backend tests | `apps/api/tests/` (105 test files; executed counts in §5) |
| Frontend | `apps/web/src/`, `apps/web/package.json`, `apps/web/package-lock.json`, Vite/TS configs |
| Frontend tests | `apps/web/src/__tests__/` (50 test files) |
| Mobile | `apps/mobile/` (Expo app, `package.json`, `package-lock.json`, tests) |
| Docker / infra | `infrastructure/docker/*.yml` (dev/staging/production/base), `infrastructure/nginx/*`, `infrastructure/scripts/*` (deploy, rollback, backup, restore, init-db, seed-data, verify-health), `infrastructure/monitoring/*`, `infrastructure/secrets/README.md` + `.gitignore` |
| CI/CD | `.github/workflows/ci.yml`, `.github/workflows/sbom_validation.yml` |
| Entrypoints | `Makefile`, `enterprise` script |
| Security / SBOM tooling | `scripts/*.sh` (python_sbom, node_sbom, _sbom_common, security-audit), `scripts/contrast-audit.js`, `scripts/evidence/`, `sbom/` package (source: `*.py`, `pyproject.toml`, `tests/`) |
| Documentation | root `*.md` (README, ARCHITECTURE, SECURITY, AUTHORIZATION, TENANCY, DEPLOYMENT, KNOWN_LIMITATIONS, CONTRIBUTING, AGENTS.md), `docs/` (38 files incl. enterprise docs), `.env.example` (root + `apps/api` + `apps/web` + `apps/mobile`) |
| Archive (read-only, intentional) | `_archive/legacy-v1/`, `_archive/backend/` — historical only, not deployed |

Total tracked: **1,256 files** (verified `git ls-files | wc -l`).

## 3. Deterministically generated but intentionally tracked

These are **generated outputs that are source-of-truth for reproducibility**
because a CI determinism gate (`sbom_validation.yml`) regenerates and diffs
them with `SOURCE_DATE_EPOCH=0`:

- `sbom/output/*.json` — CycloneDX 1.5 + SPDX 2.3 SBOMs, dependency
  inventories, license/risk/duplicate summaries, warnings (9 files).
  Regenerate: `bash scripts/python_sbom.sh && bash scripts/node_sbom.sh &&
  python -m sbom.cli generate --output-dir sbom/output`.

## 4. Generated artifacts excluded from source (gitignored — never committed)

Per `.gitignore` (rule added in `f4d7fce`, present at tag candidate):

| Category | Paths |
|---|---|
| Environment/secrets | `.env`, `.env.*` (except `.env.example`), `infrastructure/secrets/*` content |
| Python | `__pycache__/`, `*.py[cod]`, `*.egg-info/`, `.venv/`, `venv/`, `*.db` |
| Caches | `.pytest_cache/`, `.mypy_cache/`, `.ruff_cache/`, `node_modules/` (root + apps), `*.tsbuildinfo` (see §6 — currently tracked, must be untracked) |
| Evidence artifacts | `artifacts/security/*.json`, `artifacts/sbom/*.json`, `artifacts/sbom/validate.txt`, `artifacts/tests/*.xml`, `artifacts/SHA256SUMS`, `artifacts/artifact-manifest.json`, `artifacts/tests/test-manifest.json` — generated by `make security-audit` / `./enterprise audit`; only `artifacts/README.md` + `.gitkeep`s are tracked |
| Build output | `apps/web/dist/` (untracked), `dist/`, `build/`, `coverage/` |

**Security checks (verified this pass):** no private keys in the tracked tree,
no tracked `.env` files, `.env.example` templates contain placeholders only.

## 5. Release evidence requirements

Evidence must be **regenerated at the tag commit**, not carried over from an
older run (the `.gitignore` intentionally keeps evidence out of git; CI and
`make security-audit` produce it):

1. `make security-audit` → `artifacts/` (Bandit, pip-audit, npm audit,
   Gitleaks, SBOM validate, JUnit test evidence, SHA-256 manifest).
   Last known state: Bandit 0 HIGH / 20 MEDIUM, pip-audit 0, npm-audit web 0.
2. Full API suite: `cd apps/api && uv run pytest tests -q -m "not integration"`
   → last verified **1,652 passed / 0 failed** at `77c72f8`; targeted re-runs
   this cycle: async-hardening+outbox **59/0**, multi-tenant+isolation+
   migration-workspace **128/0**.
3. Web: `cd apps/web && npm test` (520 passed) + `npx tsc --noEmit` (clean).
4. Migrations: `alembic heads` = single head `049_widen_audit_action`;
   fresh-database `alembic upgrade head` on PostgreSQL 16 and SQLite.
5. Zero-touch deployment: `docker compose up --build` → postgres/redis/
   migration-init/api/worker/web/nginx healthy (verified at this commit in the
   acceptance pass; Docker daemon was down during this capture, so re-run
   before tagging).
6. Demo seed: `./enterprise demo-seed` → 3 tenants, deterministic/idempotent,
   API-level tenant-isolation tests green.
7. Document the run in `docs/enterprise/RELEASE-READINESS-REPORT.md`.

## 6. Required cleanup before tagging (do not commit yet)

**Accidental artifacts currently tracked — must be removed from the index:**

| File | Why |
|---|---|
| `apps/api/_audit_protection.txt` | audit scratch output |
| `apps/api/_audit_routes.txt` | audit scratch output |
| `apps/api/_bandit_out.json` | scanner scratch output |
| `apps/api/_pip_audit_env.json` | scanner scratch output |
| `apps/web/tsconfig.tsbuildinfo` | TypeScript incremental-build cache (regenerated every build) |

```bash
git rm --cached apps/api/_audit_protection.txt apps/api/_audit_routes.txt \
  apps/api/_bandit_out.json apps/api/_pip_audit_env.json apps/web/tsconfig.tsbuildinfo
# add cache pattern to .gitignore (already ignores *.tsbuildinfo? verify — if not, add it)
```

**Not blockers (decision recorded):**
- `.opencode/opencode.json` — dev-tool config; harmless, keep (or remove if
  the acquirer's policy forbids agent tooling).
- `_archive/` — intentionally tracked historical reference; keep.
- `sbom/output/*.json` — intentionally tracked (determinism gate); keep.
- Root `node_modules/` on disk — gitignored, delete locally before packaging.

**Release commit sequence (after approval):**
1. Apply §6 cleanup.
2. Commit: `chore: remove generated/scratch files from tracking for release`
   plus the two enterprise docs (`RELEASE-BASELINE.md`, `RELEASE-MANIFEST.md`).
3. Re-run §5 evidence at the resulting commit.
4. Tag `v2.0.0-rc.1`.

---

*This manifest is part of the release candidate documentation set
(`docs/enterprise/`). Companion: `RELEASE-BASELINE.md` (state frozen at
`77c72f8`).*
